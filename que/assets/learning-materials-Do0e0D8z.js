const e=[{id:"que-at-a-glance",title:"Que at a Glance",subtitle:"A typed, expression-oriented Lisp for vector-first data programs.",summary:"Que is a statically typed Lisp-style language with Hindley-Milner inference, homogeneous vectors, fixed-shape tuples, explicit effects, and a WebAssembly backend.",tags:["overview","que","lisp","wasm","starter"],icon:"learn",content:["Que programs are sequences of expressions. The final expression is the program result.","A call is prefix: `(f a b)`. Calls are curried/left-associated, so partial application is normal.","Prefer grouped lambda syntax in new code: `(lambda (a b) body)`. The older compact syntax `(lambda a b body)` exists, but grouped params are clearer and easier for tools and AI assistants.","Use `let` for immutable bindings and `letrec` for recursive functions. Use `mut`/`alter!` only for local primitive scalar mutation.","Use vectors `[1 2 3]` for homogeneous sequences. Strings are `[Char]`. Use tuples `{a b}` for fixed-shape products with possibly different types.","`do` sequences expressions but does not create scope. Use `block` when you need fresh local names.","The main backend emits Wasm. `que explain` shows optimized shape, effects, imports, allocations, dynamic apply calls, and vector get checks.","Side-effecting functions must end with `!`, including mutation wrappers and host I/O functions."],examples:[{title:"Small program",description:"The last expression is the result.",code:`(let x 11)
(let y 31)
(+ x y)`},{title:"Function and call",description:"Grouped lambda params are the preferred style.",code:`(let add (lambda (a b) (+ a b)))
(add 10 32)`},{title:"Vector-first style",description:"Strings are char vectors, so vector functions work on text too.",code:`(let word "owl")
{ (length word) (reverse word) (map upper word) }`}]},{id:"application-and-evaluation",title:"Application and Evaluation",subtitle:"Prefix calls, currying, partial application, and returned values.",summary:"Function application is uniform and expression based. Supplying fewer arguments returns a function waiting for the rest.",tags:["application","evaluation","partial","currying"],icon:"functions",content:["A call has the shape `(<function> <arg1> <arg2> ...)`.","`(f a b c)` behaves like `(((f a) b) c)`, so `(f a)` can be reused as a partial function.","Do not use foreign Lisp forms such as `define`, `begin`, `cond` with parentheses around clauses, `nil?`, or `null?`.","A bare function value can be returned or passed to another function.","A partial helper like `(map square)` is useful as a pipe stage, but it is not a complete program by itself unless the result is intentionally a function.","Top-level `let` forms define names; the final non-definition expression is what gets printed by normal execution."],examples:[{title:"Partial application",description:"`add10` is a function created by applying one argument.",code:`(let add (lambda (a b) (+ a b)))
(let add10 (add 10))
(add10 5)`},{title:"Function as data",description:"Functions can be passed to higher-order helpers.",code:`(let apply-twice (lambda (f x) (f (f x))))
(apply-twice square 3)`},{title:"Complete map call",description:"`map` needs both the function and the data unless used as a pipe stage.",code:"(map square [1 2 3 4])"}]}],t=JSON.parse('[{"id":"bindings-control-and-scope","title":"Bindings, Control, and Scope","subtitle":"Use `let`, `do`, `block`, `if`, `cond`, and `while` correctly.","summary":"Que is expression-oriented. Scope is explicit: `do` sequences but `block` scopes.","tags":["let","do","block","if","cond","while","scope"],"icon":"bookmark","content":["`(let name value)` binds one immutable name to one expression.","`(do e1 e2 ... en)` evaluates expressions in order and returns `en`.","`do` does not create a new scope. Reusing a `let` name in the same function body can collide.","`block` creates a fresh scope and returns its last expression. Use it when branches need local names with the same spelling.","`(if cond then else)` chooses between two expressions. Branches must have the same type.","`if` can be used with no explicit else for side effects; the missing branch is `nil`.","`unless` is like inverted `if`: `(unless cond then)` or `(unless cond then else)`. It is not a variadic body form; wrap multiple expressions in `do` or `block`.","`cond` is a flat sequence of condition/result pairs followed by a default expression: `(cond c1 e1 c2 e2 default)`.","`while` can take one body expression. Use `do` for multiple side effects or use loop macros for simpler counted loops."],"examples":[{"title":"Do returns last expression","description":"`do` is sequencing, not scoping.","code":"(do\\n  (let subtotal 48.0)\\n  (let tax (*. subtotal 0.2))\\n  (+. subtotal tax))"},{"title":"Block scopes names","description":"Use `block` when two branches need the same local names.","code":"(let pick (lambda (flag)\\n  (if flag\\n    (block (let x 10) (+ x 1))\\n    (block (let x 20) (+ x 1)))))\\n{ (pick true) (pick false) }"},{"title":"Unless with side effects","description":"Multiple expressions belong inside `do`.","code":"(let out [])\\n(mut seen false)\\n(unless seen\\n  (do\\n    (push! out 10)\\n    (alter! seen true)))\\nout"},{"title":"Cond","description":"Conditions and results are flat, ending with a default.","code":"(let score 91)\\n(cond\\n  (>= score 90) \\"A\\"\\n  (>= score 80) \\"B\\"\\n  \\"C\\")"}]},{"id":"functions-recursion-and-destructuring","title":"Functions, Recursion, and Destructuring","subtitle":"Grouped lambdas, `letrec`, tuple/vector patterns, and rest patterns.","summary":"Use grouped lambdas for normal functions, `letrec` when a function calls itself, and destructuring patterns when shapes are known.","tags":["lambda","letrec","destructuring","recursion","patterns"],"icon":"functions","content":["Preferred function syntax is `(lambda (arg1 arg2) body)`.","A grouped lambda body may contain multiple expressions; the final expression is returned.","Recursive functions must use `letrec`, not `let`.","Tuple destructuring works in `let` and params: `{a b}`.","Vector destructuring works in `let` and params: `[a b c]`.","Use `_` to skip a slot in destructuring.","Use explicit vector rest with a dot: `[head . tail]` or `[a b . rest]`.","Tuple destructuring is positional. It is not label-based.","If you destructure in a hot loop, keep the data shape simple and stable; vectors are better for homogeneous fixed-size records like `[dst src len]`."],"examples":[{"title":"Grouped lambda","description":"The body has two expressions and returns the last one.","code":"(let weighted\\n  (lambda (value weight)\\n    (let scaled (*. value weight))\\n    (+. scaled 10.0)))\\n(weighted 4.0 2.5)"},{"title":"Recursive function","description":"A function that calls itself must use `letrec`.","code":"(letrec sum-array\\n  (lambda (xs)\\n    (if (= (length xs) 0)\\n        0\\n        (+ (car xs) (sum-array (cdr xs))))))\\n(sum-array [1 2 3 4])"},{"title":"Vector and tuple destructuring","description":"Patterns bind by position.","code":"(let [a _ c] [10 20 30])\\n(let {x y} {1 2})\\n{ a c (+ x y) }"}]},{"id":"types-signatures-and-operators","title":"Types, Signatures, and Operators","subtitle":"Primitive types, suffix operators, `sig`, and `letype`.","summary":"Que infers types, but signatures can assert intended types before a binding.","tags":["types","operators","sig","letype","hm"],"icon":"type_inference","content":["Primitive types are `Int`, `Dec`, `Bool`, `Char`, and `Nil`/`nil` for unit-like values.","A vector `[T]` is homogeneous. A tuple `{A B}` is fixed-shape and can mix types.","String is `[Char]`. Character literals use single quotes, like `\'a\'`; strings use double quotes, like `\\"abc\\"`.","`\'\'` is available as the empty char value, equivalent to char code `0`.","Use `=` for Int, `=.` for Dec, `=#` for Char, and `=?` for Bool.","Decimal operators use `.` suffixes: `+.`, `-.`, `*.`, `/.`, `<.`, `>.`, `<=.`, `>=.`.","Char operators use `#` suffixes: `=#`, `<#`, `>#`, `+#`, `-#`.","Bool equality is `=?`; logical helpers include `and`, `or`, and `not`.","Use `(sig name Type)` or `(letype name Type)` before the matching binding in the same scope to assert a type. They do not create runtime values.","Use `_` in signatures as an unspecified slot, for example `(sig f (Int -> _ -> Bool))`.","Generic names like `T` and `K` are type variables in signatures. Reusing the same name means the same type must be used in those positions."],"examples":[{"title":"Typed arithmetic","description":"Suffixes select the intended primitive type.","code":"{\\n  (+ 1 2 3)\\n  (+. 1.5 2.25)\\n  (=# \'a\' \'a\')\\n  (=? true false)\\n}"},{"title":"Signature assertion","description":"The binding must match the asserted type.","code":"(sig add (Int -> Int -> Int))\\n(let add (lambda (a b) (+ a b)))\\n(add 2 3)"},{"title":"Generic signature","description":"The same generic variable must stay consistent.","code":"(sig pair-same (T -> T -> { T T }))\\n(let pair-same (lambda (a b) { a b }))\\n(pair-same 1 2)"}]},{"id":"vectors-strings-tuples-and-text","title":"Vectors, Strings, Tuples, and Text","subtitle":"Core data structures and practical text parsing.","summary":"Vectors are the main data structure. Strings are vectors of chars. Tuples are fixed product values.","tags":["vectors","strings","tuples","text","parsing"],"icon":"data_array","content":["Vectors use square brackets: `[1 2 3]`.","Strings are `[Char]`, so `length`, `get`, `map`, `filter`, `reverse`, `slice`, and `split` work on strings.","Tuples use curly braces: `{name age}`. They are positional product values, not dynamic maps.","Use `fst` and `snd` for pair tuples. For larger tuples, destructuring is clearer.","Use `split`, `split/lines`, `split/words`, and `split/commas` for common text parsing.","Use `join`, `join/lines`, and `join/commas` for text output.","Use `String->Integer`, `String->Dec`, `Integer->String`, and `Dec->String` for conversions.","Use `String/find` to find a string inside a vector of strings; it returns the index or `-1`.","Prefer vectors for homogeneous records such as `[dst src len]`; prefer tuples when fields have different types."],"examples":[{"title":"Basic vector access","description":"Use `length`, `get`, `car`, and `cdr`.","code":"(let xs [10 20 30 40])\\n{ (length xs) (get xs 2) (car xs) (cdr xs) }"},{"title":"Parse numbers from text","description":"Split by commas, then map conversion.","code":"(let input \\"10,20,30\\")\\n(|> input\\n    (split/commas)\\n    (map String->Integer)\\n    sum)"},{"title":"Tuple destructuring","description":"Destructure larger tuples instead of nesting `fst` and `snd`.","code":"(let row { \\"alice\\" 42 true })\\n(let {name score active} row)\\n{ name score active }"}]},{"id":"pipelines-and-collections","title":"Pipelines and Collections","subtitle":"Map, filter, reduce, ranges, zip, tables, sets, heaps, and matrix helpers.","summary":"Que code often reads as vector pipelines. Use the public helpers for clarity; the optimizer handles common map/filter/reduce shapes.","tags":["pipe","map","filter","reduce","range","table","set","heap"],"icon":"table_chart","content":["Pipe syntax is prefix: `(|> value stage1 stage2 stage3)`. Do not write `value |> stage` or `->>`.","A pipe stage may be a function name like `sum`, or a parenthesized partial call like `(map square)`.","Use `map`, `filter`, `reduce`, `some?`, `every?`, `find`, `count`, `sum`, `product`, `maximum`, `minimum`, `mean`, and `median` for common sequence work.","`range/inclusive` includes the end. `range/exclusive` stops before the end.","`zip` is positional: inputs should have matching dimensions. `cartesian-product` creates every pair combination.","`Table/frequency` counts character occurrences directly. `Table/count` can count boxed/hashable keys such as `(map box text)`.","Sets use `Set/new`, `Set/add!`, `Set/has?`, `Set/values`, `Set/union`, `Set/intersection`, `Set/difference`, and `Set/xor`.","Heaps use `Heap/push!`, `Heap/pop!`, `Heap/peek!`, and `Vector->Heap` with a comparison function.","Matrix helpers include `Matrix/new`, `Matrix->String`, `Matrix`-style nested vectors, and `loop/in/matrix` for imperative traversal."],"examples":[{"title":"Pipeline","description":"Data is first, stages follow left to right.","code":"(|> [1 2 3 4 5 6]\\n    (filter odd?)\\n    (map square)\\n    sum)"},{"title":"Ranges","description":"Inclusive and exclusive names make the end behavior explicit.","code":"{\\n  (range/inclusive 1 5)\\n  (range/exclusive 1 5)\\n}"},{"title":"Zip vs cartesian product","description":"Zip aligns; cartesian-product pairs everything.","code":"{\\n  (zip {[1 2] [10 20]})\\n  (cartesian-product [1 2] [10 20])\\n}"},{"title":"Frequency","description":"Count repeated boxed character keys.","code":"(Table/entries (Table/frequency \\"aaabbc\\"))"}]},{"id":"mutation-effects-and-loops","title":"Mutation, Effects, and Loops","subtitle":"When to use `mut`, `&mut`, vector mutation, and loop macros.","summary":"Que is expression-oriented, but it supports explicit mutation for hot loops, builders, and I/O.","tags":["mutation","effects","while","loop","imperative"],"icon":"bolt","content":["Use `mut` and `alter!` for local primitive scalar variables: `Int`, `Dec`, `Bool`, or `Char`.","Use `&mut`, `&get`, and `&alter!` for boxed mutable cells, especially when a value must be shared across lambdas or can hold managed values.","Use `set!`, `push!`, and `pop!` for vector mutation.","Functions that mutate state or perform I/O must end with `!`.","If a function mutates an argument, the mutated argument should be the first parameter. This keeps effect inference and alias tracking simple.","Use `while` for explicit loops. Use `(loop i (< i n) body...)` for a simple zero-based loop that increments `i` by one.","Use `(loop/range/exclusive i start end body...)` for `start <= i < end` and `(loop/range/inclusive i start end body...)` for `start <= i <= end`.","Use `(loop/in item xs body...)` or `(loop/in/vector item xs body...)` to iterate items. Use `(loop/in/matrix item matrix body...)` for nested vectors.","There is no `continue` or `break` keyword. Use `if` to skip work, or alter the loop variable/sentinel to finish early."],"examples":[{"title":"Local scalar mutation","description":"Use `mut`/`alter!` for primitive loop counters and accumulators.","code":"(do\\n  (mut i 0)\\n  (mut total 0)\\n  (while (< i 5)\\n    (do\\n      (alter! total (+ total i))\\n      (++ i)))\\n  total)"},{"title":"Vector builder","description":"Push into a vector from a counted loop.","code":"(let out [])\\n(loop/range/exclusive i 0 5\\n  (push! out (* i i)))\\nout"},{"title":"Boxed shared cell","description":"Use `&mut` when a lambda needs to update captured state.","code":"(do\\n  (&mut total 0)\\n  (each [1 2 3] (lambda (x) (&alter! total (+ (&get total) x))))\\n  (&get total))"}]}]'),s=JSON.parse('[{"id":"macros","title":"Macros","subtitle":"Compile-time syntax transforms with quote, quasiquote, unquote, and gensym.","summary":"Macros transform Que syntax before type inference. They are powerful but should be small and predictable.","tags":["macros","qq","uq","uqs","gensym"],"icon":"auto_fix","content":["Top-level macros use `(letmacro name ...)`.","Macro bodies run at compile time and return syntax.","Use `qq` to build syntax, `uq` to insert one syntax value, and `uqs` to splice many syntax values.","Use `gensym` for generated local names to avoid collisions.","Multi-clause macros dispatch by arity and shape: `(letmacro name ((a) ...) ((a b) ...))`.","Variadic macro params use dot rest: `(lambda x . body ...)`.","`when` is variadic and wraps body forms. `unless` is not variadic; it has condition plus branch expression(s).","Macros run before type inference, so generated code must still type-check normally.","Use `macroexpand-1` and `macroexpand` to inspect generated code.","Prefer macros for syntax convenience, not runtime abstraction. Runtime behavior belongs in functions."],"examples":[{"title":"Simple macro","description":"Build a syntax form with `qq` and insert the argument with `uq`.","code":"(letmacro inc/static\\n  (lambda x\\n    (qq (+ (uq x) 1))))\\n(inc/static 41)"},{"title":"Safe generated names","description":"Use `gensym` when a macro introduces bindings.","code":"(letmacro twice/static\\n  (lambda expr\\n    (do\\n      (let tmp (gensym))\\n      (qq (do\\n            (let (uq tmp) (uq expr))\\n            (+ (uq tmp) (uq tmp)))))))\\n(twice/static (+ 20 1))"},{"title":"Unless body","description":"Wrap multiple branch expressions in `do`.","code":"(unless false\\n  (do\\n    (print! \\"running\\")\\n    nil))"}]},{"id":"type-signatures-letype-and-sig","title":"Type Signatures with sig and letype","subtitle":"Assert inferred types without replacing inference.","summary":"`sig` is short syntax for `letype`. It forces a binding in the same scope to match the declared type.","tags":["sig","letype","type","generic","inference"],"icon":"rule","content":["Write `(sig name Type)` before `(let name value)` or `(letrec name value)` in the same scope.","`sig` desugars to `letype`; both are declarations, not runtime values.","If the binding does not match, type inference reports an error at compile time.","Function types use arrows: `(Int -> Int -> Int)`.","Tuple types use braces: `{ Int [Char] Bool }`.","Vector types use brackets: `[Int]`, `[[Char]]`, `[T]`.","Use `_` for a type slot you do not want to specify.","Generic names are meaningful. `T -> T` means input and output share a type. `T -> K` means they may differ.","A signature does not coerce values; it only checks that inference agrees."],"examples":[{"title":"Function signature","description":"The lambda must match the declared shape.","code":"(sig add (Int -> Int -> Int))\\n(let add (lambda (a b) (+ a b)))\\n(add 1 2)"},{"title":"Partial type assertion","description":"Use `_` to leave a slot inferred.","code":"(sig keep-left (Int -> _ -> Int))\\n(let keep-left (lambda (a b) a))\\n(keep-left 10 \\"ignored\\")"},{"title":"Generic relationship","description":"Using `T` twice requires the same type in both places.","code":"(sig wrap (T -> [T]))\\n(let wrap (lambda (x) [x]))\\n(wrap \'a\')"}]},{"id":"host-io-permissions-and-streaming","title":"Host I/O, Permissions, and Streaming","subtitle":"Read files, stdin, chunks, lines, printing, time, and permission flags.","summary":"Host functions are explicit and effectful. Run with the matching `--allow` permissions.","tags":["io","permissions","stdin","read","print","time","chunks"],"icon":"terminal","content":["Host I/O functions end with `!`: `read!`, `stdin!`, `read/chunks!`, `read/lines!`, `stdin/chunks!`, `write!`, `print!`, `sleep!`, `time!`, `clear!`.","Permissions are split by capability. Use `--allow read`, `--allow stdin`, `--allow print`, `--allow write`, `--allow sleep`, or the exact permissions your program needs.","`read!` reads a whole file as `[Char]`.","`stdin!` reads all piped stdin as `[Char]`.","`read/chunks! path size fn` streams file chunks. The callback receives `[Char]` and returns `Bool`; returning `true` stops early.","`stdin/chunks! size fn` streams stdin chunks with the same stop-early rule.","`read/lines! path fn` calls the callback for each line and stops early when it returns `true`.","`write! path text` writes text. `print! text` writes to stdout. `println!` may be implemented as a small helper/macro using `print!` plus newline.","`time!` returns the current Unix time in seconds as `Int`. `sleep!` sleeps for a number of milliseconds.","If the LSP recognizes a host function but runtime says it is unknown, rebuild/reinstall the CLI and make sure you are running the expected `que` binary."],"examples":[{"title":"Read stdin","description":"Run with `--allow stdin`.","code":"(let input (stdin!))\\n(length input)"},{"title":"Stream chunks","description":"Return `false` to keep reading, `true` to stop.","code":"(do\\n  (&mut total 0)\\n  (stdin/chunks! 65536\\n    (lambda (chunk)\\n      (do\\n        (&alter! total (+ (&get total) (length chunk)))\\n        false)))\\n  (&get total))"},{"title":"Read lines","description":"Line callbacks can stop early.","code":"(do\\n  (let found [])\\n  (read/lines! \\"input.txt\\"\\n    (lambda (line)\\n      (if (> (length line) 0)\\n          (do (push! found line) true)\\n          false)))\\n  found)"}]},{"id":"testing-and-projects","title":"Projects and Tests","subtitle":"que.toml, init, folder tests, and explain.","summary":"A Que folder can have an entry file and companion tests. Tests are appended after the entry so they can call its functions.","tags":["project","test","que.toml","explain","init"],"icon":"science","content":["`que init` creates an empty project config and starter README. It does not create a test file by default.","`que init --demo` creates runnable demo code and tests.","A simple project config can use `entry = \\"main.que\\"` and `deps = []`.","`que` run in a project folder uses the configured entry file.","`que test .` looks for project tests such as `main.test.que` and appends them after `main.que`, so tests can call entry functions.","A test file normally returns a vector of `{name result}` tuples, where each result is a Bool.","Keep tests in a separate `.test.que` file so test-only imports and helpers do not become part of normal program execution.","Use `que explain` for optimization and effects without running the program. Use `que explain --json` for machine-readable output.","Use `--emit source`, `--emit wat`, `--emit split-wat`, `--emit wasm`, and `--emit types` to inspect compiler output.","Use `--opt` for performance-oriented runs with debug checks disabled. Use debug/guard flags when you want traps for unsafe arithmetic or bounds behavior."],"examples":[{"title":"main.que","description":"Entry file defines normal functions and final output.","code":"(let add (lambda (a b) (+ a b)))\\n(add 1 2)"},{"title":"main.test.que","description":"Tests are appended after entry code, so `add` is in scope.","code":"[\\n  { \\"adds positive numbers\\" (= (add 1 2) 3) }\\n  { \\"adds zero\\" (= (add 0 5) 5) }\\n]"},{"title":"Project config","description":"Minimal `que.toml`.","code":"entry = \\"main.que\\"\\ndeps = []\\n\\n[env]\\nQUE_WASM_OPT = \\"speed\\""}]},{"id":"tables-sets-heaps-and-big-numbers","title":"Tables, Sets, Heaps, and Big Numbers","subtitle":"Useful higher-level standard-library structures.","summary":"The language core stays small, while the std library provides practical data structures on top of vectors.","tags":["table","set","heap","bigint","structdec"],"icon":"database","content":["Tables are library data structures, not syntax-level maps. Use `Table/new`, `Table/set!`, `Table/get`, `Table/get-unsafe`, `Table/has?`, `Table/entries`, `Table/keys`, and `Table/values`.","`Table/frequency` is the direct helper for character frequency. `Table/count` counts general hashable/vector-like keys such as words, and raw characters can be counted with `(Table/count (map box text))`.","`Table/frequency` is the convenience helper for occurrence tables.","Sets use `Set/new`, `Set/add!`, `Set/has?`, `Set/values`, and set algebra helpers.","Heaps use `Heap/push!`, `Heap/pop!`, `Heap/peek!`, and `Vector->Heap`.","`BigInt/*` works on non-negative big integers represented by digit vectors.","`SignedBigInt/*` works on signed big integers and prints with `SignedBigInt->String`.","`StructDec/*` and `StructBigDec/*` represent decimal values structurally when built-in `Dec` precision is not enough.","For fixed homogeneous records of big integers, vectors like `[dst src len]` are usually simpler than tuples because every field has the same type."],"examples":[{"title":"Frequency table","description":"Box characters when counting character occurrences.","code":"(Table/entries (Table/frequency \\"aaabbc\\"))"},{"title":"Set values","description":"Convert vector-like keys into a set and inspect values.","code":"(Set/values (Vector->Set [\\"a\\" \\"b\\" \\"a\\"]))"},{"title":"Signed BigInt","description":"Use the signed wrapper for negative values.","code":"(SignedBigInt->String\\n  (SignedBigInt/mul\\n    (SignedBigInt/new \\"-12\\")\\n    (SignedBigInt/new \\"30\\")))"}]},{"id":"pitfalls-for-new-users-and-ai","title":"Pitfalls for New Users and AI Assistants","subtitle":"Common wrong guesses and the correct Que forms.","summary":"Most beginner errors come from importing assumptions from other Lisps or JavaScript-style pipeline syntax.","tags":["pitfalls","ai","syntax","debugging"],"icon":"warning","content":["Do not write `define`, `begin`, `fn`, `nil?`, `null?`, `list`, `->`, `->>`, or infix pipe syntax.","Use `(lambda (args...) body)`; `fn` is not a keyword.","Use `(= (length xs) 0)` or `empty?`, not `nil?`, to test an empty vector.","Use `letrec` for recursive functions. `(letrec (f ...))` is wrong; write `(letrec f value)`.","`let` binds exactly one name to exactly one expression: `(let x 1)`, not `(let (x 1) body)`.","Pipe syntax is prefix: `(|> xs (map f) (filter p) sum)`. Do not write `xs |> map f` or `(->> xs ...)`.","A pipe stage like `(map square)` is a parenthesized partial call. Do not split it into bare tokens as `map square` inside `|>`.","`do` is not a scope. Use `block` for branch-local names.","`unless` takes branch expressions like `if`; it is not a variadic body list.","`zip` is positional and expects matching dimensions. Use `cartesian-product` for all pair combinations.","Vectors are homogeneous. Use tuples for mixed fixed-shape values.","A generic parser for arbitrary mixed data needs an encoded representation or future ADTs; without sum types, parse known schemas into known static types."],"examples":[{"title":"Correct recursion","description":"Use `letrec name value`.","code":"(letrec fact\\n  (lambda (n)\\n    (if (= n 0)\\n        1\\n        (* n (fact (- n 1))))))\\n(fact 5)"},{"title":"Correct pipe","description":"Stages are function names or parenthesized partial calls.","code":"(|> [1 2 3 4 5]\\n    (filter odd?)\\n    (map square)\\n    sum)"},{"title":"Correct scoping","description":"Use `block` to reuse names in branches.","code":"(if true\\n  (block (let x 1) x)\\n  (block (let x 2) x))"}]}]'),n=[{id:"commands-and-flags",title:"Commands and Flags",subtitle:"Run, test, explain, emit, init, and permissions.",summary:"The CLI can run programs, test projects, inspect compiler output, and scaffold projects/hosts.",tags:["cli","commands","flags","permissions"],icon:"terminal",content:["Run a file: `que main.que`.","Run the project entry in the current folder: `que`.","Evaluate source: `que --eval '(+ 1 2)'`.","Run tests: `que test .`.","Inspect optimized program shape: `que explain` or `que explain --json`.","Emit compiler output: `que main.que --emit source|wat|split-wat|wasm|types --out out.file`.","Create a project: `que init`; create a demo project: `que init --demo`.","Scaffold a custom Rust host executable: `que init-host <name>`.","Enable permissions explicitly: `--allow read`, `--allow stdin`, `--allow print`, `--allow write`, `--allow sleep`, etc.","Use `que --help` for CLI details and `que --learn` for the compact language reference."],examples:[{title:"Run with stdin",description:"The permission must match the host function.",code:"printf 'abc\\n' | que main.que --allow stdin"},{title:"Emit WAT",description:"Inspect generated Wasm text.",code:"que main.que --emit wat --out out.wat"},{title:"Explain",description:"Get optimization and effect information without running.",code:`que explain
que explain --json`}]},{id:"standard-library-highlights",title:"Standard Library Highlights",subtitle:"Small, useful groups of functions for daily Que programs.",summary:"This is not exhaustive, but it lists the names new users reach for most often.",tags:["stdlib","reference","functions"],icon:"library_books",content:["Sequences: `map`, `filter`, `reduce`, `map/i`, `reduce/i`, `length`, `get`, `car`, `cdr`, `cons`, `first`, `last`, `reverse`, `slice`, `take/first`, `drop/first`, `find`, `some?`, `every?`.","Ranges: `range/inclusive`, `range/exclusive`, `range`, `range.`.","Integer math: `+`, `-`, `*`, `/`, `mod`, `odd?`, `even?`, `zero?`, `positive?`, `negative?`, `square`, `expt`, `sqrt`, `max`, `min`, `abs`, `sum`, `product`, `maximum`, `minimum`, `mean`, `median`, `clamp`.","Decimal math: `+.`, `-.`, `*.`, `/.`, `mod.`, `odd.?`, `even.?`, `zero.?`, `positive.?`, `negative.?`, `expt.`, `sqrt.`, `floor`, `ceil`, `log`, `sin`, `cos`, `max.`, `min.`, `abs.`, `sum.`, `product.`, `mean.`, `avg.`, `median.`.","Text conversion: `String->Vector`, `Vector->String`, `String->Integer`, `String->Dec`, `Integer->String`, `Dec->String`, `Integer->Digits`, `Digits->Integer`, `Digits->Chars`.","Text splitting/joining: `split`, `split/lines`, `split/words`, `split/commas`, `join`, `join/lines`, `join/commas`, `replace`.","Tuples: `tuple`, `fst`, `snd`, `pair`, tuple destructuring with `{a b}`.","Tables/Sets/Heaps: `Table/*`, `Set/*`, `Heap/*`, `Vector->Set`, `Vector->Heap`.","Big numbers: `BigInt/*`, `SignedBigInt/*`, `StructDec/*`, `StructBigDec/*`."],examples:[{title:"Average",description:"Convert length to decimal for decimal division.",code:`(let avg
  (lambda (xs)
    (/. (Int->Dec (sum xs)) (Int->Dec (length xs)))))
(avg [1 2 3 4])`},{title:"Words",description:"Parse words and count them.",code:`(let words (split/words "one two two three"))
(Table/entries (Table/count words))`},{title:"Matrix string",description:"Nested char vectors can be joined for output.",code:'(Matrix->String [["a" "b"] ["c" "d"]])'}]}],a={concepts:e,core:t,references:s,tools:n};export{e as concepts,t as core,a as default,s as references,n as tools};
